project.version=1.0
src.dir=${basedir}/src
lib.dir=${basedir}/lib
ext.dir=${basedir}/ext
build.dir=${basedir}/build
build.repo.dir=${build.dir}/repo
build.classes.dir=${build.dir}/classes
build.jar.dir=${build.dir}/jars
test.dir=${basedir}/test
tools.dir=${basedir}/tools

build.coverage.dir=${build.dir}/coverage
build.test.classes.dir=${build.dir}/test-classes
build.coverage.dir=${build.dir}/coverage
build.coverage.data.file=${build.dir}/cobertura.ser
build.instrumented.classes.dir=${build.dir}/instrumented-classes

javac.target=1.5

model.client.dir=${basedir}/mock-sdk-local-client
model.client.lib.dir=${model.client.dir}/lib
model.client.conf.dir=${model.client.dir}/conf
model.src.dir=${basedir}/mock-sdk-model
build.model.classes.dir=${build.dir}/model-classes
